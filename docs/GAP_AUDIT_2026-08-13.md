# ATROPOS — Exhaustive Unimplemented Inventory

**Audited at:** commit `a327249` · 2026-08-13
**Authority:** Source Docs 1–4 · 100% Completion Blueprint · Core Engine Gap Map v2 · HOE UI/UX Gap Map v2 · Phase 20 Architecture Gap Map v2 · Superiority Addendum
**Tree measured:** 771 main Kotlin files · 306 test files · 202 registered commands

## How this was produced

Every component named in the source documents was searched for by symbol, then —
where found — checked for **production callers outside its own file**. That second
check is the one that matters. A component with a file and a passing test and zero
callers is not implemented; it is a stub with better camouflage, and it reads as
green on every metric that counts files or tests.

The gap maps are dated 2026-07-29. Substantial work has landed since, so their
status columns are stale in both directions — several atoms they mark OPEN are
closed, and several they assume present are orphaned. This document reflects the
tree, not the maps.

Four categories are used, ordered by how misleading each is:

| Category | Meaning |
|---|---|
| **DEFECT** | Claimed working; observed failing on the device |
| **ORPHANED** | Code + tests exist, zero production callers |
| **ABSENT** | No code at all |
| **UNENFORCED** | Exists, wired, but default-off or advisory |

---

## 1. DEFECT — currently failing on the installed runtime

These are worse than missing, because the system reports them as soft-failures and
continues, so a run looks successful.

| # | Defect | Evidence | Blast radius |
|---|---|---|---|
| D1 | `LocalMemoryStore.search` throws `StackOverflowError` | `st_memory=SKIPPED_SOFT_FAIL:stackoverflowerror` and `lt_memory=` same, live run 2026-08-13 | Both memory channels dead. Phase 9 acceptance ("memory survives restart, remains queryable") cannot be true. Every factory run plans with zero recall. |
| D2 | Lakehouse per-atom retrieval returns `status=MISS` on every path | live run; `path=N/build/build_systems status=MISS` ×3 | Atoms generate uninformed. Chain reaches the store and comes back empty. |
| D3 | DLOI resolves nothing on real prompts | `dloi=SKIPPED_SOFT_FAIL:no_exact_match` | Phase 6 acceptance ("every executable requirement has a durable source coordinate") unproven in practice. |
| D4 | `provider_suggestions=SKIPPED_SOFT_FAIL:confidence_threshold_met` | live run | Provider stage is skipped whenever local confidence is high, so the provider path is rarely exercised end-to-end. |
| D5 | Soft-fail is the normal outcome, not the exception | 5 of 6 research channels soft-failed in the last observed run | Blueprint requires acceptance gates; a run where every channel degrades still reports `confidence=100`. The confidence calculation does not reflect channel health. |

---

## 2. ABSENT — no code exists

### 2.1 Evaluation subsystem (Source Doc 3 §4, Blueprint Phase 20)

The evaluation package contains 4 files and covers 17 **gate-shaped booleans**.
None of the specified *metrics* exist. There is no metric that produces a number.

| Missing | Required by |
|---|---|
| `AtroposMetrics` (one file per metric family) | SD3 §4.3 |
| `MetricCalculator` | SD3 §4.3 |
| `BenchmarkRunner` | SD3 §4.3, item 56 |
| `EvidenceStore` | SD3 §4.3 |
| `ClassificationCalculator` | SD3 §4.3 |
| Restart recovery success **rate** | SD3 §4.1, item 57 |
| Verifier-first catches before LLM escalation | SD3 §4.1 |
| Coordination efficiency (tokens per verified change) | SD3 §4.1 |
| Territory safety **percentage** | SD3 §4.1 (only a boolean `TERRITORY_EVIDENCE` exists) |
| Identity recognition accuracy | SD3 §4.1, item 58 |
| Context attestation success **rate** | SD3 §4.1 |
| Drift detection latency | SD3 §4.1 |
| Trace completeness | SD3 §4.1 |
| Copy fidelity | SD3 §4.1 |
| Preview success rate | SD3 §4.1 |
| Event determinism | SD3 §4.1 |
| Repair quality (permanent vs recurring) | SD3 Part C §7 |
| Batch completion rate / rollback frequency | SD3 Part C §7 |
| Provider route effectiveness over time | SD3 Part C §7 |
| Five-way release classification (score reduction / minimum / competitive / frontier / safety hard) | SD3 §4.2, item 60 — only `INFO`/`WARNING`/`BLOCKER` exist |
| Zero-target division fix in lower-is-better metrics | item 59 |
| SWE-bench Verified · Terminal-Bench · Aider Polyglot · PR acceptance · time-to-accepted-PR | item 56 |
| Evaluation CLI + dashboard | item 65 |

### 2.2 Observability / provenance (Source Doc 3 §5, items 12–16)

Nothing in this section exists under the specified names. `EventJournalService`
and `RunObserver` are the nearest owners and neither satisfies the contract.

| Missing | Required by |
|---|---|
| `ExecutionEvent` | SD3 §5.1 |
| `ProvenanceStream` | SD3 §5.1 |
| `EventPublisher` / `EventSubscriber` | SD3 §5.1 |
| `OutputCard` / `CardRenderer` | SD3 §5.2, item 13 |
| `MarkdownExporter` / `JsonExporter` (full-run export) | SD3 §5.2, item 14 |
| `ExecutionHistoryStore` / `HistoryQuery` / `HistoryIndex` | SD3 §5.3, item 16 |
| Searchable history that survives restart without loading the whole trace | item 16 |

### 2.3 Phase 20 self-improvement loop

Component files exist (`core/phase20/`, 9 files, 6 test files). **The loop is
never assembled.** `ProposalGenerator`, `ProposalGate` and `ReproducibilityGate`
have exactly one production reference between them, inside `EvaluationEngine`,
and nothing drives the transition chain.

| Missing | Required by |
|---|---|
| `SelfImprovementLoop` | SD3 Part C §6, Blueprint Phase 20 |
| `AutonomousBacklogManager` | SD3 Part C §6 (`AutonomousBacklog` exists but is not the restart-safe manager) |
| `PolicyGate` | SD3 Part C §6 |
| Full transition chain P20-L01 → L15 | P20 gap map §2 — **all 15 OPEN** |
| Lakehouse ledgers P20-LH01 → LH07 (Evidence / Memory / Proposal / Amendment ledgers as CAS objects with structural manifests) | P20 gap map §3 — **all 7 OPEN** |
| Governance candidates P20-G01 → G09 | P20 gap map §4 — **all 9 OPEN** |
| Hard boundaries P20-H01 → H06 (rate/depth/budget limits, cooldown, observation period, quarantine, termination ranking) | P20 gap map §5 — **all 6 OPEN** |
| Laws 20.1 → 20.20 as enforced predicates | P20 gap map §1 — **all 20 OPEN** |
| `[NON-SOURCE]` primitives P20-NS01 → NS07 (proof-carrying amendments, formal `R(d)`, metric-space `I(p)`, termination ranking, object/meta separation, proposal lattice, unified CAS substrate) | P20 gap map §6 — **all 7 OPEN** |
| Continuous superiority P20-S01 → S04 | P20 gap map §7 — **all 4 OPEN** |
| First canonical amendment: *nonzero compile/test exit forbids VERIFIED* | P20 gap map, named as the first target |

### 2.4 Phase 19 App Factory — deployment half

Planning classes exist (`AppAuthPlanner`, `AppDatabaseSecurityPlanner`,
`AppBackendIntegrationPlanner`). Everything past *planning* is missing.

| Missing | Required by |
|---|---|
| `DeploymentService` | Blueprint Phase 19 capability list |
| Hosting / preview / live environments | Blueprint Phase 19 |
| Domains + HTTPS | Blueprint Phase 19 |
| Release rollback | Blueprint Phase 19 |
| `RepositoryBinding` as its own owner | Blueprint §7 (referenced inside `ProjectRegistry`, not extracted) |
| Scheduled / background tasks, realtime behavior | Blueprint Phase 19 |
| Activity monitor unifying plan/provider/tool/diff/test/verifier/artifact/deploy | Blueprint Phase 19, C3-P19 |

### 2.5 Intent layer (Source Doc 3 §6, items 36–54)

| Missing | Required by |
|---|---|
| `ActionRegistry` | SD3 §6.1 |
| `AliasResolver` | SD3 §6.1, item 40 |
| `CommandConsolidator` | SD3 §6.1, item 39 |
| `CommandMetadata` (central structured metadata) | SD3 §6.1, item 53 |
| 13 canonical verbs as the contract surface | SD3 §6, Section B |
| Natural-language phrase mappings (item 51) | SD3 item 51 |
| Inline argument guidance (item 50) | SD3 item 50 |

`IntentParser` exists twice — `core/factory/IntentParser.kt` (39L) and a second
under planning. Neither is the SD3 §6 contract layer.

### 2.6 Source authority

| Missing | Required by |
|---|---|
| `SourceDocumentRegistry` | Blueprint Phase 6 gap closure — named explicitly, does not exist |

### 2.7 Superiority Addendum — remaining atoms

| Atom | Status |
|---|---|
| `SUP.UX.ANSI-SCHEME-TOKENS` | `AnsiScheme.kt` ABSENT. `DesignTokens`/`Role`/`ThemePalette` cover it in spirit; the addendum's named single-owner and the "raw escape codes forbidden outside scheme" compliance assertion do not exist. |
| `SUP.STOR.GLOBAL-BYTE-CEILING` | ABSENT. `StorageAccountant` + `StorageConstitution` exist; the declared ceiling and refusal-on-exceed do not. |
| `SUP.PROV.ZERO-HARDCODED-PATHS` | Partially met — 0 absolute path literals remain — but `PathResolver` as a named owner is ABSENT and no `ArchitectureComplianceChecker` assertion enforces it. |
| `SUP.INT.COMPUTER-USE-BRIDGE` | ABSENT (`ComputerUseBridge`) |
| `SUP.UX.SESSION-TAB-DENSITY` | `SessionManager` ABSENT |
| `SUP.UX.RECOVERY-RIBBON` | `RecoveryRibbon` ABSENT |
| `SUP.ART.AT-MENTION-UPLOAD` | No `@mention` ingestion path |
| `SUP.VERIF.TERRITORY-MONITOR-COST` | No cost counters on `TerritoryGrantService`; O(N) vs O(N²) claim undocumented in evidence bundles |
| `SUP.PROV.SURFACE-PARITY` | `SurfaceContract` exists but runs on one surface only — parity is asserted, not tested across surfaces |

---

## 3. ORPHANED — file and tests exist, zero production callers

**This is the most dangerous category in the tree.** Each of these passes its
tests, appears in the symbol census, and satisfies a blueprint line item by name
while being unreachable from any running code path.

| Component | Prod callers | Tests | Consequence |
|---|---:|---:|---|
| `LivePreviewService` | **0** | 1 | Phase 17/19 live preview + hot reload is dead code. `hotReload()` is defined and never invoked. |
| `VisualComparison` | **0** | **0** | Before/after visual comparison (item 22) has neither caller nor test. |
| `EvidenceCollector` | **0** | 1 | Blueprint names it as the unifier of journal/verification/artifact/run-observer evidence. Nothing constructs it. |
| `McpTerritoryBridge` | **0** | 1 | MCP surface exists as a class inside `InboundToolRequest.kt`; no server registers it. |
| `CapabilityEnforcer` | 1 | **0** | Enforces capability restrictions with no test coverage at all. |

Single-caller components below are wired but only from one site — verify the
caller is a live path and not another orphan before trusting them:
`GraphClaimService`, `GraphTransitionService`, `BoundedWorkExecutor`, `BatchGate`,
`PlanningGraphPluginRegistry`, `DagNodeRestorer`, `ReleaseGateEvaluator`,
`AntiGamingAuditor`, `ProposalGenerator`, `ProposalGate`, `GovernanceLedger`,
`SurfaceContract`, `NlCanonicalizer`, `LocalNlResolver`, `InterruptController`,
`OutputValidator`, `IndependentVerificationGate`, `GitHubBinding`.

---

## 4. UNENFORCED — exists but default-off or advisory

| Item | Current state | Required |
|---|---|---|
| `ArchitectureComplianceChecker` | `enforcing = false` by default | Blueprint §5 and SD3 §1.2: advisory *then* **blocking**. Never became blocking. |
| Extreme per-file atomic decoupling | **55 files over 250 lines**, 4 over 400 | SD3 §1: one file = one atomic responsibility |
| Phase 20 governance | Reachable only through the bridge projection; **no CLI surface** | P20 laws must gate real transitions |
| `enforcement mode` for territory | `TerritoryEnforcer` returns `false` on empty territory (fail-closed, good) but is called from 3 sites only | Blueprint Phase 13: check create/edit/delete/rename/shell **before** mutation |

### Largest atomicity violations

```
413  cli/CommandRouter.kt              ← SD3 §10 names this explicitly
407  core/factory/FactoryRunEventRecorder.kt
403  core/autonomous/AutonomousOrchestrator.kt
401  core/factory/AppProjectGenerator.kt
369  bridge/BridgeRoutes.kt
365  dloi/SourceAuthorityIndexer.kt
361  cli/input/CommandCatalog.kt
360  core/provider/ProviderActivationService.kt
348  Main.kt
347  core/factory/SpecGraphAtomizer.kt
```

---

## 5. Surfaces

### CLI/TUI — closest to done

Open HOE atoms: `HOE-B08` (copy/download from within CLI — no clipboard path),
`HOE-B09` (territory-as-material / attestation optical focus), `HOE-E01`,
`HOE-E02`, `HOE-E06` (recovery ribbon), `HOE-E07` (mode retheme from status).
`HOE-B11` baseline snapshots exist for 40/80/120/160 but are stale relative to the
current renderer.

### Web — shell exists, engine contract does not

From the delta register, still open:

| Row | Gap |
|---|---|
| `UI-DELTA-WEB-027` | **Bridge endpoints not implemented**: `POST /session`, `GET /session`, `GET /session/:id`, `POST /message`, `GET /events` (SSE), `POST /approve`, `POST /reject`, `GET /evidence/:id`, `GET /files` |
| `UI-DELTA-WEB-024` | Multi-view project (Conversation / Timeline / Execution Monitor) |
| `UI-DELTA-WEB-020` | `How?` pipeline field on engine entities |
| `UI-DELTA-WEB-015` | Approvals never erase history — **BLOCKED** |
| `UI-DELTA-WEB-003` | Unrestricted argv passthrough on `POST /cli` — this is an RCE surface |
| `UI-DELTA-WEB-016`, `-022` | Project model ownership + creation affordance — PARTIAL |
| `HOE-0011` | No PTY over the bridge |

Note: `apps/atropos-web/` — the directory the HOE handoff and the gap map both
name as ATROPOS-owned — **does not exist**. The live app is `apps/web/`, which
carries Supabase auth routes. Every path reference in `WEB_MERGE_ARCHITECTURE.md`
and `HOE-C01`/`HOE-C07` points at a directory that is not in the tree, so those
atoms cannot be worked as written without first resolving the ownership boundary.

### Android — shell exists, HOE chrome does not

33 Kotlin files under `app/`, real Compose shell, signed-APK CI workflow.
`UI-DELTA-AND-001` still **DEFERRED**: Claude-density primary column, approval
cards, push on approval-needed, one-hand density, offline project resume.

### Desktop — not started

No Compose Desktop target, no `desktopMain`. Blueprint Phase 18 requires it;
the HOE gap map defers it until CLI+Web+Android are green.

---

## 6. Acceptance proofs never demonstrated

The blueprint's definition of 100% is proof-based, not code-based. None of the
seven Phase 20 crossover proofs has been run end-to-end on the installed runtime:

1. **Self-host proof** — ATROPOS closes a real ATROPOS requirement through its own loop and runs the newly installed JAR. *Sandbox proof exists; interactive-JAR mutation + git push path is unproven.*
2. **Greenfield factory proof** — one prompt creates, previews, tests, commits, packages **and deploys** a full-stack app. *Deployment half absent (§2.4).*
3. **Long-horizon proof** — multi-document workload continues across interruptions until all runnable nodes verify. *Never run.*
4. **Recovery proof** — process death restores exact claim, territory, worktree, evidence pointers, next action. *`RestartCoordinator` exists; the proof has not been executed.*
5. **Safety proof** — drift, secret leakage, out-of-territory change, self-verification, fake success, policy bypass all blocked. *No adversarial suite.*
6. **Fallback proof** — planning plugins and cloud providers disappear without destroying core planning. *Untested.*
7. **Learning proof** — evidence changes bounded routing/retry/batch weights while invariants hold. *Requires §2.3, which does not exist.*

Also unproven: **Phase 0** — fresh clone on Termux and clean CI producing the
same accepted JAR hash. Nothing records or compares JAR fingerprints across the
two environments.

---

## 7. Suggested order

The dependency order that unblocks the most, in sequence:

1. **D1** (memory StackOverflow) — cheapest fix, unblocks Phase 9 acceptance and every factory run's recall.
2. **D2** (lakehouse MISS) — the retrieval chain is built and reaches the store; only the last hop fails.
3. **§3 orphans** — wire `EvidenceCollector`, `LivePreviewService`, `VisualComparison`. These are already written; wiring them is small and converts four blueprint line items from false-green to true.
4. **§2.2 observability** — `ExecutionEvent` + `ProvenanceStream` + exporters. Everything in Phase 20 evaluation reads from this; building evaluation first would mean building it twice.
5. **§2.1 evaluation metrics** — now has a data source.
6. **§2.3 Phase 20 loop** — now has metrics to compute `I(p)` against.
7. **Web bridge endpoints** (`UI-DELTA-WEB-027`) — unblocks both Web and Android, since Android consumes the same bridge.
8. **§4 enforcement** — flip `ArchitectureComplianceChecker` to blocking only after the 55 oversized files are split, or the build stops immediately.

---

*Method note: "prod callers" counts files outside the component's own definition
that construct it. A component whose only caller is another orphan is still
unreachable; the single-caller list in §3 has not been transitively resolved.*

---

# Part 2 — Source Docs 1, 2 and 4, and the SD3 74-item list

The first pass audited the Blueprint, Source Doc 3 and the three v2 gap maps.
It did not open Source Docs 1 and 2, where the DLOI address map, the mathematical
execution constraints, the swarm IPC protocol, the multi-platform migration plan
and the 30-provider grid live. This part covers those, plus the two enumerated
requirement lists (SD3's 74 items, SD4's `HOE-0001`–`0020`) item by item.

## 8. Source Doc 1

### 8.1 DLOI Master Address Map (§.001) — ABSENT as a system

`OntologicalAddressRouter` is **29 lines**: it parses `"NNN.NNN.NNN"` into a
triple and formats it back. It does not implement the address map at all.

| Missing | Spec |
|---|---|
| DLOI band scheme (000–019 META · 004/005/006 · 400–439 DATA & NETWORKING · 500 · 510–529 INTELLIGENCE · 620–629 APPLIED RUNTIME) | §.001 "DLOI BAND SCHEME" |
| CORPUS → DLOI table (010 git, 005 roadmap, 006 security, 410 supabase, 521 compilers, 522 eslint, 620 kotlin, 622 java, 624 gradle) | §.001 |
| Bundle resolution (`B1_devtools`, `B2_ai_stack` resolving to multiple domains, disambiguated by CATEGORY/heading) | §.001 |
| Routing protocol: intent → DOMAIN codes → open only those sources + matching AST slice | §.001 |
| Multi-domain request → BLUEPRINT first (DAG), pull per task, in phases | §.001 |
| Reserved coordinates `420` (async sockets) and `621` (Android Kotlin runtime) | §.001 |
| `UnixDomainSocketBridge` (`420.011`, the spec's own worked example) | §.001, §1.0.1 |
| Companion docs: `ATROPOS_AGENT_PLAYBOOK`, `ATROPOS_HASH_INDEX.txt`, `ATROPOS_MAP_2_DLOI_COORDINATES` | §.001 |

### 8.2 `ast_symbol_graph` SQLite schema (§1.0.1) — ABSENT

The document gives an exact DDL. No table matching it exists; the identifier
appears only inside `WorkbenchTruthProbe.kt`.

```sql
CREATE TABLE ast_symbol_graph (
  node_id TEXT PRIMARY KEY,      -- SHA-256 of symbol signature
  dloi_address TEXT NOT NULL,    -- 9-digit taxonomic coordinate
  symbol_type TEXT NOT NULL,     -- CLASS | INTERFACE | METHOD | FIELD
  file_path TEXT NOT NULL,
  byte_offset_start INTEGER,
  byte_offset_end INTEGER,
  dependency_refs TEXT           -- JSON array of node_ids
);
CREATE INDEX idx_dloi ON ast_symbol_graph(dloi_address);
```

`idx_dloi` does not exist anywhere in the tree. **AST_NAMESPACE_RECONCILER**
("models do not guess package imports; the reconciler queries the DLOI index and
injects the absolute string path deterministically") has no implementation
against a DLOI-addressed index.

### 8.3 Mathematical execution constraints (§1.0.1) — ABSENT as enforced law

| Missing | Spec |
|---|---|
| State-Aligned MDP against a non-differentiable deterministic compiler | §1.0.1 |
| Topological mutation vectors `Δt` between structural snapshots (modifications isolated as graph deltas, **not parsed as strings**) | §1.0.1 — current patch path is textual |
| Tree Edit Distance context stripping targeting ~94.2% prompt-weight saving | §1.0.1. `CodebaseDeltaTreeTracker` (274L) exists; no measured saving, not on the prompt path |
| `E(Δ) = HIG + HUD = 0` as a committed-state precondition | §1.0.1 — `BatchGate` implements `E(Δ)=0` for *territory* only, not for HIG/HUD |
| Error-gradient extraction: slice the failing sub-graph, route only the broken signature + exact stderr coordinate back | §1.0.1 |
| Sequential Monte Carlo program sampling / branch pruning on compiler log interception | §1.0.1 DEEP_RESEARCH |
| Decomposed attention: separate "Viewer Node" (navigates symbol graph) from "Editor Node" (receives slice) | §1.0.1 DEEP_RESEARCH |

### 8.4 Swarm IPC protocol (§1.0.1, §1.0.2) — ABSENT

The three-node fan-out/fan-in topology is specified with named engines and
input/output states. `DirectorOrchestrator` and `WorkerCodeSynthesizer` were
deleted (correctly, per Blueprint §4.5) and **nothing replaced them**.

| Missing | Spec |
|---|---|
| `NODE_ALPHA_DIRECTOR` — strategic supervisor emitting topologically ordered DAG task lists from raw specifications | §1.0.1 |
| `NODE_BETA_WORKER_SYNTHESIZER_CLUSTER` — concurrent fan-out compiling separate files simultaneously without blocking | §1.0.1 |
| `NODE_GAMMA_VALIDATOR_GUARD` — on-device adversarial evaluator returning `Boolean(Syntax_Valid) + Missing_Imports[]` **before cloud tokens are spent** | §1.0.1 |
| Async fan-out/fan-in decoupling to bypass context dilution | §1.0.1 |
| 5-API delegation matrix with execution planes and P0/P1 priority | §1.0.2 §2 |
| Orchestrator emitting a JSON manifest of execution nodes ordered by topological prerequisites | §1.0.2 §1.1 |

### 8.5 Dopamine circuit (§1.0.1, §1.0.2 §3, §1.0.3 Part 3) — PARTIAL

`RewardPenaltyStore` is **81 lines**. The specified contract is not met.

| Missing | Spec |
|---|---|
| `RewardVector(score, trace)` type with `+1.0` on exit 0, `-1.0` + stderr trace otherwise | §1.0.2 §3.1 (worked code given) |
| `success_weights.db` as the named persistent store | §1.0.3 Part 3 |
| `Reward = SuccessRate / (Latency × Cost)` metric | §1.0.3 Part 3 |
| `DatabaseProvider.kt` handling write-heavy logging **without blocking the main event loop** | §1.0.3 Part 3 |
| Closed-loop alignment: auto-tune prompt prefix templates, temperature, top_p and few-shot examples from rolling success rate | §1.0.1 RLHF_PIPELINE |

### 8.6 CAS / lakehouse replication (§1.0.1) — PARTIAL

| Missing | Spec |
|---|---|
| Memory-mapped I/O (mmap) via Android NDK | §1.0.1 — no `mmap` anywhere |
| Shadow-log replication of quantized embedding shards | §1.0.1 |
| 5 TB global matrix mapped to local storage in mmap chunks | §1.0.1 |
| Global dedupe of identical SDK dependencies across projects | §1.0.1 |

### 8.7 AGPL enforcement perimeter (§1.0.1) — PARTIAL

| Missing | Spec |
|---|---|
| Cloud-loophole closure: architecture continuously evaluates its own deployment to detect UI-stripped remote hosting | §1.0.1 |
| Native intercept shell wrapper — bare `git`, `cd`, `gh` intercepted as host commands | §1.0.1. Only `/git`, `/cd`, `/ls`, `/shell` slash forms exist; bare commands are not intercepted |
| Piped input (`\|`) and async process streams in the router | §1.0.3 Part 4 |

### 8.8 Multi-platform migration (§.005) — mostly ABSENT

| Target | State |
|---|---|
| KMP `core` module | **ABSENT** — single JVM module; `build.gradle.kts` has no `kotlin("multiplatform")` |
| Module split `core/ cli/ desktop/ androidApp/ server/ shared-ui/` | **ABSENT** — `app/` exists for Android only |
| Compose Desktop | **ABSENT** |
| GraalVM Native Image | **ABSENT** |
| Dockerfile (JVM + native variants, health checks, graceful shutdown) | **ABSENT** — no Dockerfile in tree |
| Ktor backend | **ABSENT** — the bridge is a hand-rolled HTTP handler set |
| iOS / Compose for iOS | **ABSENT** |
| Platform abstraction interfaces `LocalToolchain` / `Renderer` / `InputSystem` | `PlatformAbstraction` exists; the three named interfaces do not |
| Installers (`.deb`, Homebrew, Scoop) | **ABSENT** |

## 9. Source Doc 2

Most of Source Doc 2 is process law for the human/agent workflow rather than
runtime code. The runtime-bearing parts:

### 9.1 Provider grid (§.300 §4) — 30 IDs specified

Registered ids found: 30 distinct strings, but not the specified set.

| Missing / mismatched | Note |
|---|---|
| `LOCAL_TOOLCHAIN` (id 0, quota weight 0, **root** of every chain) | Not a registered provider. It is the root of `REPAIR_CHAIN`, `DOCS_CHAIN`, `SEARCH_CHAIN`, `EMBED_CHAIN`, `MEMORY_CHAIN`, `SECRET_CHAIN`, `EDGE_CHAIN`, `ASSET_CHAIN` and `remote_compile` — nine chains root at a provider that is not in the grid. |
| `CUSTOM_USER_API` (id 29, adapter slot, must declare cost/quota before use) | ABSENT |
| `openrouter_free` vs registered `openrouter` | The spec distinguishes free-tagged models and requires **model rotation when rate-limited**; no rotation logic exists |
| Quota-weight ordering 0/1/2/3/4-5/6-8/9 as the primary sort key | `quota_weight ASC` is the first sort term in §7; not implemented as specified |

### 9.2 Provider eligibility algorithm (§.300 §7) — PARTIAL

Nine eligibility predicates and six sort terms are specified exactly. Not
implemented as a single evaluable rule. Specifically absent:
`remaining_estimate DESC`, `recent_success_score DESC`, `latency_estimate ASC`,
`cooldown_risk ASC`, and the `model_missing` state.

Failure-state transitions are specified per error class
(`429` → cooldown from headers · quota → `exhausted_until_reset` + `reset_at` ·
billing → `billing_required` + never auto-retry · auth → `auth_failed` ·
model missing → alternate model then fallback · timeout → short cooldown ·
malformed → log and next). These are not all typed states in the ledger.

### 9.3 Task routing matrix (§.300 §5) — ABSENT as data

14 task types × 6 columns (First/Second/Third/Fourth/Degraded-Queue/Paid) are
specified as a table. There is no routing table in the codebase carrying these
rows; routing is coded rather than declared, so a change to the matrix is a code
change.

### 9.4 Eleven named fallback chains (§.300 §6) — ABSENT as data

`CHAT_CHAIN`, `CODE_CHAIN`, `REPAIR_CHAIN`, `PLANNING_CHAIN`, `DOCS_CHAIN`,
`SEARCH_CHAIN`, `EMBED_CHAIN`, `MEMORY_CHAIN`, `SECRET_CHAIN`, `EDGE_CHAIN`,
`ASSET_CHAIN` — none exist as named, inspectable chains.

### 9.5 Workflow rules with runtime consequences (§.320, §.220, §.225)

| Rule | State |
|---|---|
| 23 — use a `Clock` interface | One usage found (`CheckpointAge`); not a systemic injected clock |
| 121 — every assertion names a unique invariant + observed value; bare `check(condition)` prohibited | Not enforced; no lint for bare `check` |
| 124 — output passes `{interactive-color, interactive-NO_COLOR, TERM=dumb, headless}` matrix | `NO_COLOR` handled in 11 files; the four-way matrix is not a test |
| 127 — snapshot output as **both** raw bytes and ANSI-stripped at widths 40/80/120 | Baselines exist as stripped text only |
| 129 — artifacts compiled to a temp dir, moved into place only after every gate passes | `SafeJarSwapGate` exists; temp-dir-then-move is not the build's shape |
| 131 — batches report physical lines, code-bearing lines, additions, deletions | ABSENT |
| 134 — pre-batch scan for risky stdlib calls (`Sequence.takeLast`, `kotlin.io.path`, kotlinx deps, Java above target) | ABSENT — no compiler-compatibility scanner |
| 137 — `SUCCESS=1` only after kotlinc 0 + smoke 0 + grep truth + `git diff --check` 0 + jar swap | Partially in scripts; not a single enforced gate |
| 142–147 — auto-emit next minimal context-export command; export to phone Downloads; `termux-media-scan` | ABSENT |

## 10. Source Doc 3 — the 74-item list, items not covered in Part 1

| # | Item | State |
|---|---|---|
| 24–26 | Browser-style persistent tabs · per-tab state · **tab restoration across restart** | Tabs exist; **no restore path** — 0 files reference tab snapshot/restore |
| 28 | Responsive branding — logo never clipped, including the cut-off "POS" | No test asserts the brand survives at 40 cols |
| 32–33 | Real activity indicators · **no fake progress** (animations must correspond to persisted process state) | Spinner is not bound to persisted state; no guard |
| 34 | Background-process panel (running jobs, children, elapsed, logs, progress, owner, status, cancel) | **ABSENT** |
| 35 | Distinguish running / waiting / blocked / retrying / stalled / failed / cancelled / complete | **stalled** has no representation anywhere |
| 44 | Touch autocomplete — tap suggestions above the keyboard | **ABSENT** |
| 46 | Safe fuzzy execution — uncertain matches shown for confirmation, never silently executed | `FuzzyMatcher` exists; the confirmation gate does not |
| 47 | Context-sensitive suggestions that change with screen/task/failure/patch/build | **ABSENT** |
| 50 | Inline argument guidance (expected args, available values, examples) | **ABSENT** |
| 59 | Repair the zero-target division flaw in lower-is-better metrics | **ABSENT** (no such metrics exist yet) |
| 68 | Accessibility — focus visibility, **screen-reader labels**, **reduced-motion**, contrast checks | 0 files reference screen-reader labels, reduced motion, or high contrast |
| 69 | Performance — **virtualized long logs**, bounded rendering, controlled background updates | 0 files implement virtualization |
| 71 | Canonical acceptance tests for context recognition, territory blocks, autocomplete, tabs, responsive rendering, previews, exports, recovery, process visibility | Partial; no single named suite |

## 11. Source Doc 4 — `HOE-0001`–`0020` seeds

Beyond the `HOE-A/B/C/D/E/F` atoms in Part 1 §5:

| ID | State |
|---|---|
| `HOE-0003` | **MISSING** in the delta register — "no durable project store on either surface" |
| `HOE-0011` | **MISSING** — terminals first-class and linkable to evidence; no PTY over the bridge |
| `HOE-0016` | **PARTIAL** — Android shell real, HOE chrome absent |
| `HOE-0004` | Status vocabulary exists (`HoeStatusVocabulary.kt`) but **color-independence is unverified** — no non-color channel test |
| `HOE-0008` | Progressive disclosure L1–L4: L1/L2/L3 exist in the thinking stream; **L4 Internal / Developer Tools level does not** |
| `HOE-0014` | Developer Tools inspectors — Runtime · Agent · Provider · Policy · Source Authority · Recovery. **None of the six exist as named inspectors** |
| `HOE-0019` | Accessibility gates — see item 68 above, unenforced |

## 12. Source Doc 3 Section D — the 16 views

Section D specifies per-view layout, states, primary verbs, data-source mapping
and motion for 16 views. **None exists as a view in any surface** except as CLI
renderers that partially cover the data. Named and unbuilt:

`GOVERNANCE` (6 tabs + cross-tab alert rail) · `PIPELINE/ARTIFACTS` (Guided/Manual
mode switch, 6 stage cards) · `DAG` (force-directed canvas, cycle outline) ·
`SNAPSHOTS` (gallery + compare) · `SECURITY` (Keys/Redaction/Vault sub-tabs with
paste-in redaction preview) · `MEMORY` · `PAID/EMERGENCY` (lift-cover physical
metaphor, app-wide danger banner while unlocked) · `VERIFY` (Narrow/Wide scope
toggle, pass/fail matrix) · `SOURCE LOOKUP` (single box, auto-typed results) ·
`AUTONOMOUS` ("who's driving" indicator) · `SWARM` (honest not-yet-available card)
· `JOBS/QUEUE` (unified CI + agent queue) · `PLATFORM`.

Also absent from Section B/E:
- **13 canonical verbs as the contract surface** — `CanonicalVerb.kt` exists as an enum; no view renders "valid verbs on this object"
- **`contract/` module** — client/server split where `ui-` modules depend only on `contract/`
- **MVI per screen** (`Intent → Reducer → State`, `StateFlow<ScreenState>`)
- **Local HTTP+SSE server** binding `127.0.0.1:<port>` with optional password, one route per backend operation generated from `OperationRegistry`
- **Design tokens**: corner-radius concentricity (parent-radius-minus-inset), 44×44pt tap targets, 8pt grid, the ten semantic colour roles with light/dark/**tinted**
- **The five Section E acceptance tests**, notably #5: *adding an `OperationEndpoint` with `configured=true` produces a new Home card and a reachable view with zero UI code changes* — the literal test that the decoupling architecture is real

---

## 13. Revised order

Part 1's order still holds for the engine. Part 2 adds three items that belong
early because everything downstream assumes them:

1. **`ast_symbol_graph` + `idx_dloi`** (§8.2) — Phase 7's acceptance, the AST
   namespace reconciler, and the DLOI↔code shared address space all require it.
   `TreeSitterGrammarBridge` is real now (141L) and has nowhere to write.
2. **DLOI band scheme + routing protocol** (§8.1) — this is why `dloi=` returns
   `no_exact_match` on every prompt (defect D3). The router parses coordinates it
   has no map for.
3. **`LOCAL_TOOLCHAIN` as provider 0** (§9.1) — nine fallback chains root at it.

Everything in §8.8 (KMP, Docker, GraalVM, Ktor, Desktop, iOS) is genuinely
deferred work and should not be scheduled against the current phase gates.

---

## 14. AGENTS.md control document (Baseline 42)

`AGENTS.md` is present at repo root (9,199 lines) with §0–§10 intact and **811
progress-ledger rows** appended. The control contract is being honoured.

Two gaps against it:

| Gap | Detail |
|---|---|
| **`Swarm.md` does not exist** | `SwarmMdLoader.kt` (114L) is built to load it and `SUP.AUTH.SWARM-MD` requires *"missing Swarm.md on multi-agent command → explicit refusal with recovery hint"*. The loader has no document, so the refusal path is the only path that can ever run. Multi-agent topology, territory grants, escalation paths, max depth and coordination-cost bound are therefore undeclared. |
| **Baseline percentages are stale** | §1 is locked at 2026-07-29 and must never be overwritten — correct. But the per-phase figures it carries (Phase 7 AST 50%, Phase 9 Memory 60%, Phase 20 12%) predate ~90 commits. The ledger records deltas; no recomputed *current* aggregate is maintained anywhere, so "what percent is ATROPOS now" has no answer in the repo. |

`AGENTS.md` §1 also asserts `ArchitectureComplianceChecker` at 161 L with
"enforcement mode incomplete". It is now **117 L** and enforcement is still
`false` by default — see §4.

---

## 15. Document coverage

Every supplied document has been walked:

| Document | Covered in |
|---|---|
| ATROPOS 100% Completion Blueprint | Part 1 §1–§7 |
| Source Doc 1 (DLOI map · machine-state handoff · multi-platform guide) | Part 2 §8 |
| Source Doc 2 (workflow · free-first API grid · provider rules) | Part 2 §9 |
| Source Doc 3 (Part B decoupling/hierarchy · 74 items · Sections A–F) | Part 1 §2, Part 2 §10, §12 |
| Source Doc 4 (HOE constitution · handoff v2 · HOE-0001–0020) | Part 1 §5, Part 2 §11 |
| Core Engine Gap Map v2 (C1/C2/C3/C4 · CONT · NS-01–06) | Part 1 §1–§4 |
| HOE UI/UX Gap Map v2 (A/B/C/D/E/F series) | Part 1 §5 |
| Phase 20 Architecture Gap Map v2 (laws 20.1–20.20 · L01–L15 · LH01–07 · G01–09 · H01–06 · NS01–07 · S01–04) | Part 1 §2.3 |
| Superiority Addendum (SUP.AUTH/TERR/STOR/UX/PROV/VERIF/NL/INT/ART) | Part 1 §2.7 |
| AGENTS.md External Agent Control Document (Baseline 42) | Part 2 §14 |

Not supplied in this pass and therefore not audited: **Source Doc 5** (referenced
by the Superiority Addendum §5 as fully absorbed into the `SUP.*` atoms, so its
content is covered transitively) and the **MusicMakerLM blueprints** (named in
SD3 Part C §11 as the first proof workload; no ATROPOS code depends on them).
