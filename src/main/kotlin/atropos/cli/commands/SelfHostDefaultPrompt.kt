package atropos.cli.commands

/** Canonical prompt used when the operator invokes bare `self-host`. */
object SelfHostDefaultPrompt {
    const val TEXT: String = "ATROPOS, build yourself from the inside out"
}
